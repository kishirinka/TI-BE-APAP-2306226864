services:
  insurance-db:
    image: postgres:17.5-alpine
    ports:
      - "35001:5432"
    restart: "no"
    environment:
      - POSTGRES_USER=insurance-dev
      - POSTGRES_PASSWORD=CLI-825.2025X
      - POSTGRES_DB=insurance-dev
    volumes:
      - insurance-db:/var/lib/postgresql/data

volumes:
  insurance-db:
    driver: local
